Build me a complete WhatsApp AI customer service SaaS platform called “Jawab” (جواب).

## Tech Stack:

•⁠  ⁠Backend: Node.js + Express
•⁠  ⁠Database: PostgreSQL (use Replit’s built-in PostgreSQL or Neon.tech)
•⁠  ⁠Real-time: Socket.io
•⁠  ⁠WhatsApp: Twilio API
•⁠  ⁠AI: Anthropic Claude API (claude-sonnet-4-20250514)
•⁠  ⁠Auth: JWT + bcryptjs
•⁠  ⁠Frontend: React (simple dashboard)

## Environment Variables needed (I’ll add in Secrets):

•⁠  ⁠TWILIO_ACCOUNT_SID
•⁠  ⁠TWILIO_AUTH_TOKEN
•⁠  ⁠TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886
•⁠  ⁠DATABASE_URL
•⁠  ⁠ANTHROPIC_API_KEY
•⁠  ⁠JWT_SECRET
•⁠  ⁠PORT=3000

## Database Schema - Create these tables:

⁠ sql
CREATE TABLE tenants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL,
  phone VARCHAR(20),
  plan VARCHAR(50) DEFAULT 'starter',
  twilio_phone VARCHAR(20),
  ai_enabled BOOLEAN DEFAULT true,
  ai_system_prompt TEXT,
  max_agents INTEGER DEFAULT 2,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  name VARCHAR(255) NOT NULL,
  role VARCHAR(20) DEFAULT 'agent',
  status VARCHAR(20) DEFAULT 'offline',
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE contacts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE,
  phone VARCHAR(20) NOT NULL,
  name VARCHAR(255),
  tags TEXT[] DEFAULT '{}',
  notes TEXT,
  sentiment VARCHAR(20) DEFAULT 'neutral',
  total_conversations INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(tenant_id, phone)
);

CREATE TABLE conversations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE,
  contact_id UUID REFERENCES contacts(id) ON DELETE CASCADE,
  assigned_to UUID REFERENCES users(id),
  status VARCHAR(20) DEFAULT 'active',
  channel VARCHAR(20) DEFAULT 'whatsapp',
  ai_handled BOOLEAN DEFAULT false,
  started_at TIMESTAMP DEFAULT NOW(),
  resolved_at TIMESTAMP,
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  conversation_id UUID REFERENCES conversations(id) ON DELETE CASCADE,
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE,
  sender_type VARCHAR(20) NOT NULL,
  sender_id UUID,
  content TEXT NOT NULL,
  media_url TEXT,
  media_type VARCHAR(50),
  ai_confidence REAL,
  twilio_sid VARCHAR(100),
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE auto_replies (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE,
  trigger_type VARCHAR(20) NOT NULL,
  trigger_value TEXT NOT NULL,
  response TEXT NOT NULL,
  is_active BOOLEAN DEFAULT true,
  priority INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE ai_knowledge (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE,
  title VARCHAR(255),
  content TEXT NOT NULL,
  category VARCHAR(100),
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE quick_replies (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE,
  title VARCHAR(100) NOT NULL,
  content TEXT NOT NULL,
  shortcut VARCHAR(50),
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_messages_conversation ON messages(conversation_id, created_at);
CREATE INDEX idx_conversations_tenant ON conversations(tenant_id, status);
CREATE INDEX idx_contacts_tenant_phone ON contacts(tenant_id, phone);
 ⁠

## API Routes to build:

### 1. POST /api/webhook/twilio

•⁠  ⁠Receives incoming WhatsApp messages from Twilio webhook
•⁠  ⁠Parses: req.body.From, req.body.Body, req.body.MessageSid, req.body.ProfileName
•⁠  ⁠Finds or creates contact by phone number
•⁠  ⁠Finds or creates active conversation
•⁠  ⁠Saves message to database
•⁠  ⁠If AI enabled: calls Claude API with conversation history + tenant’s knowledge base to generate response
•⁠  ⁠If AI confidence > 60%: sends auto-reply via Twilio
•⁠  ⁠If AI confidence < 60%: marks conversation as “waiting” for human agent
•⁠  ⁠Emits real-time update via Socket.io to dashboard
•⁠  ⁠IMPORTANT: Respond to Twilio immediately with 200 status and empty TwiML <Response></Response>

### 2. POST /api/auth/register

•⁠  ⁠Creates new tenant + admin user
•⁠  ⁠Hashes password with bcrypt
•⁠  ⁠Returns JWT token

### 3. POST /api/auth/login

•⁠  ⁠Validates email/password
•⁠  ⁠Returns JWT token + user info

### 4. GET /api/conversations

•⁠  ⁠Returns all conversations for tenant with last message, contact info, unread count
•⁠  ⁠Filter by status (active, waiting, resolved)

### 5. GET /api/conversations/:id/messages

•⁠  ⁠Returns all messages for a conversation

### 6. POST /api/conversations/:id/messages

•⁠  ⁠Agent sends message
•⁠  ⁠Sends via Twilio WhatsApp
•⁠  ⁠Saves to database
•⁠  ⁠Emits via Socket.io

### 7. PATCH /api/conversations/:id

•⁠  ⁠Update status, assign agent

### 8. GET /api/contacts

•⁠  ⁠List contacts with search

### 9. CRUD /api/ai/knowledge

•⁠  ⁠Manage AI knowledge base entries

### 10. CRUD /api/ai/auto-replies

•⁠  ⁠Manage keyword-based auto reply rules

### 11. GET /api/conversations/stats/overview

•⁠  ⁠Dashboard stats: active, waiting, resolved counts, AI resolution rate

## AI Service:

•⁠  ⁠First check auto_replies table for keyword matches
•⁠  ⁠If no keyword match, use Claude API with this system prompt:


أنت مساعد خدمة عملاء ذكي. رد بنفس لغة العميل (عربي أو إنجليزي). كن ودوداً ومحترفاً. لو ما تعرف الجواب، قل "دقيقة وأحوّلك لموظف متخصص". لا تخترع معلومات. الردود مختصرة ومفيدة.


•⁠  ⁠Include tenant’s knowledge base entries as context
•⁠  ⁠Include last 10 messages as conversation history

## Twilio Service:

•⁠  ⁠sendWhatsAppMessage(to, body): sends message via twilio client
•⁠  ⁠parseIncomingMessage(body): extracts From, Body, MessageSid, ProfileName, MediaUrl0

## WebSocket (Socket.io):

•⁠  ⁠On connection: agent joins tenant room
•⁠  ⁠Events: new_message, escalation, ai_response, agent_typing, agent_status

## Frontend Dashboard (React):

Build an Arabic-first (RTL) dashboard with dark theme (background: #0a0f1a) and green accent (#22c55e, WhatsApp-inspired):

### Layout:

•⁠  ⁠Left sidebar (64px): Navigation icons (chat, contacts, AI, analytics, settings)
•⁠  ⁠Contact list panel (340px): Search, filter tabs (all/active/waiting/resolved), contact cards with avatar, name, last message, time, unread badge, tags
•⁠  ⁠Main chat area (flex): Chat header with contact info + AI toggle + agent assignment, messages area, input with quick replies button + attachment + send
•⁠  ⁠Right panel (300px, toggleable): Contact profile, customer info, tags, internal notes

### Features:

•⁠  ⁠Real-time messages via Socket.io
•⁠  ⁠AI messages shown with green border and confidence percentage badge
•⁠  ⁠Escalation messages shown with yellow/amber border
•⁠  ⁠Agent messages shown with blue border
•⁠  ⁠Customer messages shown with gray border
•⁠  ⁠Typing indicator animation
•⁠  ⁠Quick replies popup (lightning bolt button)
•⁠  ⁠Filter conversations by status
•⁠  ⁠Search contacts
•⁠  ⁠Toggle AI on/off per conversation
•⁠  ⁠Assign conversation to agent dropdown
•⁠  ⁠Stats bar: avg response time, AI resolution rate, active count, waiting count

### Styling:

•⁠  ⁠Font: IBM Plex Sans Arabic from Google Fonts
•⁠  ⁠Dark theme with glassmorphism effects
•⁠  ⁠Green gradient avatars for active contacts
•⁠  ⁠Smooth transitions and hover effects
•⁠  ⁠All text RTL direction

## Important:

•⁠  ⁠Make sure the Twilio webhook endpoint responds within 15 seconds
•⁠  ⁠Process AI responses asynchronously after responding to Twilio
•⁠  ⁠Use connection pooling for PostgreSQL
•⁠  ⁠Add proper error handling everywhere
•⁠  ⁠The app should work immediately after adding the environment variables